dist: xenial   # required for Python >= 3.7
language: python
python:
  - "3.7"
install:
  - "pip install -e ."
  - "pip install -q behave"
# command to run tests
script:
 - python --version
 - behave
